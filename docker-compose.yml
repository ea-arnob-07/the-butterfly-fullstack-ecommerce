services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: butterfly
      POSTGRES_PASSWORD: butterfly_password
      POSTGRES_DB: the_butterfly
    ports:
      - "5432:5432"
    volumes:
      - butterfly_postgres:/var/lib/postgresql/data

volumes:
  butterfly_postgres:
